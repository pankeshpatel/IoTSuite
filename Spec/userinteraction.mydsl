resources:
    userInteractions:
		SmartHomeApp						
			command Off() to Heater;
	 		command SetTemp(setTemp) to Heater;
	 		request profile to ProfileDB;	 	
	 		notify DisplayTemp(tempNotify:TempStruct) from DisplayTempController;
	 	